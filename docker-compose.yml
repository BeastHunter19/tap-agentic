services:
  backend:
    build: ./backend
    volumes:
      - ./backend:/app
    environment:
      - AI_API_KEY_FILE=/run/secrets/ai_api_key
    ports:
      - "8000:8000"
    secrets:
      - ai_api_key
secrets:
  ai_api_key:
    file: ./secrets/gemini_api_key.txt